export default class ArrayTest {
  /**
   * Pushes a value into an array.
   * @param {array} arr Array to add to.
   * @param {object} d Data to be added.
   */
  static push(arr, d) {
    arr.push(d);
  }
}